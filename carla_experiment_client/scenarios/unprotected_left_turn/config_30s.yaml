id: unprotected_left_turn
map: Town03
doc: docs/scenarios/unprotected_left_turn.md
weather: ClearNoon
seed: 21
duration: 60.0
fps: 20
fixed_delta_seconds: 0.05
sync_mode: true
no_rendering_mode: false
ego_vehicle: vehicle.tesla.model3
camera:
  width: 1280
  height: 720
  fov: 90
  fps: 20
  preset: driver
events:
  min_event_time_s: 30.0
  voice_lead_time_s: 3.0
  robot_precue_lead_s: 0.5
scenario:
  # Critical event at ~40s (frame 800 at 20fps)
  # Ego approaches intersection, begins left turn, must yield to oncoming traffic
  approach_frames: 780               # Ego drives on autopilot until 39s
  turn_frames: 100                   # 5s turn maneuver
  ego_throttle: 0.40                 # Moderate throttle during approach
  turn_throttle: 0.25                # Slower during turn
  turn_steer: -0.30                  # Left turn
  turn_steer_auto: true              # Auto-detect turn direction
  ego_speed_delta: 10.0              # Ego at moderate speed
  ego_follow_distance_m: 10.0
  background_vehicle_count: 12       # Reduced to minimize noise
  background_walker_count: 4
  background_min_distance_m: 40.0    # Keep background away
  prewarm_seconds: 1.0
